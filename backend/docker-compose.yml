version: '3.8'

services:
  postgres_db:
    image: postgres:latest  
    container_name: KmaERM_PostgresDB 
    environment:
      POSTGRES_USER: tuyenngduc
      POSTGRES_PASSWORD: tuyen123
      POSTGRES_DB: mydatabase 
    ports:
      - "5432:5432" 
    volumes:
      - postgres_data:/var/lib/postgresql/data 

volumes:
  postgres_data: